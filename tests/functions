map: ( N => N ) * N[] => N[];
def map(f, val) {
	?
	return [], if val == [2],
	return [f(val[0])] + map(f, val[1:]), else
	?;
};

sum: N * N => N;
def sum (x1, x2) {
	return x1 + x2;
};

let list[]: N = [1, 2, 3];

write sum(sum, list);
